# API Rails — muevetechico

API JSON para el sitio Next.js. Ver el README de la raíz del repo para instalar y correr todo junto.

```bash
bundle install
bin/rails db:prepare db:seed
PORT=43124 bundle exec rails server -b 0.0.0.0 -p 43124
```
